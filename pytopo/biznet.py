#!/usr/bin/python

"""
Custom topology for Mininet, generated by GraphML-Topo-to-Mininet-Network-Generator.
"""
from mininet.topo import Topo
from mininet.net import Mininet
from mininet.node import RemoteController
from mininet.node import Node
from mininet.node import CPULimitedHost
from mininet.link import TCLink
from mininet.cli import CLI
from mininet.log import setLogLevel
from mininet.util import dumpNodeConnections

class GeneratedTopo( Topo ):
    "Internet Topology Zoo Specimen."

    def __init__( self, **opts ):
        "Create a topology."

        # Initialize Topology
        Topo.__init__( self, **opts )

        # add nodes, switches first...
        Cilacap = self.addSwitch( 's0' )
        Purwokerto = self.addSwitch( 's1' )
        Sukabumi = self.addSwitch( 's2' )
        Tasikmalaya = self.addSwitch( 's3' )
        Pekalongan = self.addSwitch( 's4' )
        Semarang = self.addSwitch( 's5' )
        Magelang = self.addSwitch( 's6' )
        Yogyakarta = self.addSwitch( 's7' )
        Kudus = self.addSwitch( 's8' )
        Surakarta = self.addSwitch( 's9' )
        NusaDua = self.addSwitch( 's10' )
        Surabaya = self.addSwitch( 's11' )
        Kediri = self.addSwitch( 's12' )
        Madium = self.addSwitch( 's13' )
        Tuban = self.addSwitch( 's14' )
        Banyunwangi = self.addSwitch( 's15' )
        Jember = self.addSwitch( 's16' )
        Probolinggo = self.addSwitch( 's17' )
        Malang = self.addSwitch( 's18' )
        Bakauhuni = self.addSwitch( 's19' )
        Cibaliung = self.addSwitch( 's20' )
        Tegal = self.addSwitch( 's21' )
        Cirebon = self.addSwitch( 's22' )
        Bandung = self.addSwitch( 's23' )
        Karawang = self.addSwitch( 's24' )
        Jakarta = self.addSwitch( 's25' )
        Bogor = self.addSwitch( 's26' )
        Serang = self.addSwitch( 's27' )
        Anyer = self.addSwitch( 's28' )

        # ... and now hosts
        Cilacap_host = self.addHost( 'h0' )
        Purwokerto_host = self.addHost( 'h1' )
        Sukabumi_host = self.addHost( 'h2' )
        Tasikmalaya_host = self.addHost( 'h3' )
        Pekalongan_host = self.addHost( 'h4' )
        Semarang_host = self.addHost( 'h5' )
        Magelang_host = self.addHost( 'h6' )
        Yogyakarta_host = self.addHost( 'h7' )
        Kudus_host = self.addHost( 'h8' )
        Surakarta_host = self.addHost( 'h9' )
        NusaDua_host = self.addHost( 'h10' )
        Surabaya_host = self.addHost( 'h11' )
        Kediri_host = self.addHost( 'h12' )
        Madium_host = self.addHost( 'h13' )
        Tuban_host = self.addHost( 'h14' )
        Banyunwangi_host = self.addHost( 'h15' )
        Jember_host = self.addHost( 'h16' )
        Probolinggo_host = self.addHost( 'h17' )
        Malang_host = self.addHost( 'h18' )
        Bakauhuni_host = self.addHost( 'h19' )
        Cibaliung_host = self.addHost( 'h20' )
        Tegal_host = self.addHost( 'h21' )
        Cirebon_host = self.addHost( 'h22' )
        Bandung_host = self.addHost( 'h23' )
        Karawang_host = self.addHost( 'h24' )
        Jakarta_host = self.addHost( 'h25' )
        Bogor_host = self.addHost( 'h26' )
        Serang_host = self.addHost( 'h27' )
        Anyer_host = self.addHost( 'h28' )

        # add edges between switch and corresponding host
        self.addLink( Cilacap , Cilacap_host )
        self.addLink( Purwokerto , Purwokerto_host )
        self.addLink( Sukabumi , Sukabumi_host )
        self.addLink( Tasikmalaya , Tasikmalaya_host )
        self.addLink( Pekalongan , Pekalongan_host )
        self.addLink( Semarang , Semarang_host )
        self.addLink( Magelang , Magelang_host )
        self.addLink( Yogyakarta , Yogyakarta_host )
        self.addLink( Kudus , Kudus_host )
        self.addLink( Surakarta , Surakarta_host )
        self.addLink( NusaDua , NusaDua_host )
        self.addLink( Surabaya , Surabaya_host )
        self.addLink( Kediri , Kediri_host )
        self.addLink( Madium , Madium_host )
        self.addLink( Tuban , Tuban_host )
        self.addLink( Banyunwangi , Banyunwangi_host )
        self.addLink( Jember , Jember_host )
        self.addLink( Probolinggo , Probolinggo_host )
        self.addLink( Malang , Malang_host )
        self.addLink( Bakauhuni , Bakauhuni_host )
        self.addLink( Cibaliung , Cibaliung_host )
        self.addLink( Tegal , Tegal_host )
        self.addLink( Cirebon , Cirebon_host )
        self.addLink( Bandung , Bandung_host )
        self.addLink( Karawang , Karawang_host )
        self.addLink( Jakarta , Jakarta_host )
        self.addLink( Bogor , Bogor_host )
        self.addLink( Serang , Serang_host )
        self.addLink( Anyer , Anyer_host )

        # add edges between switches
        self.addLink( Cilacap , Purwokerto, bw=10, delay='0.21351287261571425ms')
        self.addLink( Cilacap , Tasikmalaya, bw=10, delay='0.5049226481539009ms')
        self.addLink( Purwokerto , Magelang, bw=10, delay='0.5516738218502182ms')
        self.addLink( Sukabumi , Bogor, bw=10, delay='0.19848037196282645ms')
        self.addLink( Sukabumi , Bandung, bw=10, delay='0.3882386782062331ms')
        self.addLink( Tasikmalaya , Bandung, bw=10, delay='0.40639768474939897ms')
        self.addLink( Pekalongan , Tegal, bw=10, delay='0.30039016066156904ms')
        self.addLink( Pekalongan , Semarang, bw=10, delay='0.42205378017410783ms')
        self.addLink( Semarang , Kudus, bw=10, delay='0.25864990055029224ms')
        self.addLink( Semarang , Surakarta, bw=10, delay='0.3928696628556855ms')
        self.addLink( Magelang , Yogyakarta, bw=10, delay='0.19376679416276854ms')
        self.addLink( Yogyakarta , Surakarta, bw=10, delay='0.29314236205410005ms')
        self.addLink( Kudus , Tuban, bw=10, delay='0.6807031454925975ms')
        self.addLink( Surakarta , Madium, bw=10, delay='0.3899605831153441ms')
        self.addLink( NusaDua , Banyunwangi, bw=10, delay='5.307797904054554ms')
        self.addLink( Surabaya , Probolinggo, bw=10, delay='0.3864699564922474ms')
        self.addLink( Surabaya , Kediri, bw=10, delay='0.5215170715588285ms')
        self.addLink( Surabaya , Tuban, bw=10, delay='0.43975301433593533ms')
        self.addLink( Kediri , Malang, bw=10, delay='0.355651138606505ms')
        self.addLink( Kediri , Madium, bw=10, delay='0.29544054523037716ms')
        self.addLink( Banyunwangi , Jember, bw=10, delay='0.37126618209526396ms')
        self.addLink( Jember , Probolinggo, bw=10, delay='0.358457705851761ms')
        self.addLink( Jember , Malang, bw=10, delay='0.6093527578872445ms')
        self.addLink( Bakauhuni , Anyer, bw=10, delay='3.9351573246634652ms')
        self.addLink( Cibaliung , Bogor, bw=10, delay='0.5360059740550918ms')
        self.addLink( Cibaliung , Anyer, bw=10, delay='0.39599401803511486ms')
        self.addLink( Tegal , Cirebon, bw=10, delay='0.3399715524388608ms')
        self.addLink( Cirebon , Bandung, bw=10, delay='0.5382315337318433ms')
        self.addLink( Bandung , Karawang, bw=10, delay='0.3821567209059054ms')
        self.addLink( Karawang , Jakarta, bw=10, delay='0.263439102310631ms')
        self.addLink( Jakarta , Bogor, bw=10, delay='0.21691449226653242ms')
        self.addLink( Jakarta , Serang, bw=10, delay='0.3944611686597282ms')
        self.addLink( Serang , Anyer, bw=10, delay='0.15240645354079468ms')

topos = { 'generated': ( lambda: GeneratedTopo() ) }

# HERE THE CODE DEFINITION OF THE TOPOLOGY ENDS

# the following code produces an executable script working with a remote controller
# and providing ssh access to the the mininet hosts from within the ubuntu vm
controller_ip = ''

def setupNetwork(controller_ip):
    "Create network and run simple performance test"
    # check if remote controller's ip was set
    # else set it to localhost
    topo = GeneratedTopo()
    if controller_ip == '':
        #controller_ip = '10.0.2.2';
        controller_ip = '127.0.0.1';
    net = Mininet(topo=topo, controller=lambda a: RemoteController( a, ip=controller_ip, port=6633 ), host=CPULimitedHost, link=TCLink)
    return net

def connectToRootNS( network, switch, ip, prefixLen, routes ):
    "Connect hosts to root namespace via switch. Starts network."
    "network: Mininet() network object"
    "switch: switch to connect to root namespace"
    "ip: IP address for root namespace node"
    "prefixLen: IP address prefix length (e.g. 8, 16, 24)"
    "routes: host networks to route to"
    # Create a node in root namespace and link to switch 0
    root = Node( 'root', inNamespace=False )
    intf = TCLink( root, switch ).intf1
    root.setIP( ip, prefixLen, intf )
    # Start network that now includes link to root namespace
    network.start()
    # Add routes from root ns to hosts
    for route in routes:
        root.cmd( 'route add -net ' + route + ' dev ' + str( intf ) )

def sshd( network, cmd='/usr/sbin/sshd', opts='-D' ):
    "Start a network, connect it to root ns, and run sshd on all hosts."
    switch = network.switches[ 0 ]  # switch to use
    ip = '10.123.123.1'  # our IP address on host network
    routes = [ '10.0.0.0/8' ]  # host networks to route to
    connectToRootNS( network, switch, ip, 8, routes )
    for host in network.hosts:
        host.cmd( cmd + ' ' + opts + '&' )

    # DEBUGGING INFO
    print()
    print("Dumping host connections")
    dumpNodeConnections(network.hosts)
    print()
    print("*** Hosts are running sshd at the following addresses:")
    print()
    for host in network.hosts:
        print(host.name, host.IP())
    print()
    print("*** Type 'exit' or control-D to shut down network")
    print()
    print("*** For testing network connectivity among the hosts, wait a bit for the controller to create all the routes, then do 'pingall' on the mininet console.")
    print()

    CLI( network )
    for host in network.hosts:
        host.cmd( 'kill %' + cmd )
    network.stop()


if __name__ == '__main__':
    setLogLevel('info')
    #setLogLevel('debug')
    sshd( setupNetwork(controller_ip) )
