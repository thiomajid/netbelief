#!/usr/bin/python

"""
Custom topology for Mininet, generated by GraphML-Topo-to-Mininet-Network-Generator.
"""
from mininet.topo import Topo
from mininet.net import Mininet
from mininet.node import RemoteController
from mininet.node import Node
from mininet.node import CPULimitedHost
from mininet.link import TCLink
from mininet.cli import CLI
from mininet.log import setLogLevel
from mininet.util import dumpNodeConnections

class GeneratedTopo( Topo ):
    "Internet Topology Zoo Specimen."

    def __init__( self, **opts ):
        "Create a topology."

        # Initialize Topology
        Topo.__init__( self, **opts )

        # add nodes, switches first...
        Eisenstadt = self.addSwitch( 's0' )
        St.Polten = self.addSwitch( 's1' )
        Graz2 = self.addSwitch( 's2' )
        Leoben = self.addSwitch( 's3' )
        Vienna2 = self.addSwitch( 's4' )
        GEANT = self.addSwitch( 's5' )
        Krems = self.addSwitch( 's6' )
        Vienna1 = self.addSwitch( 's7' )
        Level3 = self.addSwitch( 's8' )
        VIX = self.addSwitch( 's9' )
        None = self.addSwitch( 's10' )
        SANET = self.addSwitch( 's11' )
        CESNET = self.addSwitch( 's12' )
        Klagenfurt2 = self.addSwitch( 's13' )
        Graz1 = self.addSwitch( 's14' )
        Linz1 = self.addSwitch( 's15' )
        Linz2 = self.addSwitch( 's16' )
        Salzburg1 = self.addSwitch( 's17' )
        Salzburg2 = self.addSwitch( 's18' )
        Innsbruck1 = self.addSwitch( 's19' )
        Innsbruck2 = self.addSwitch( 's20' )
        Dornbirn = self.addSwitch( 's21' )
        Klagenfurt1 = self.addSwitch( 's22' )

        # ... and now hosts
        Eisenstadt_host = self.addHost( 'h0' )
        St.Polten_host = self.addHost( 'h1' )
        Graz2_host = self.addHost( 'h2' )
        Leoben_host = self.addHost( 'h3' )
        Vienna2_host = self.addHost( 'h4' )
        GEANT_host = self.addHost( 'h5' )
        Krems_host = self.addHost( 'h6' )
        Vienna1_host = self.addHost( 'h7' )
        Level3_host = self.addHost( 'h8' )
        VIX_host = self.addHost( 'h9' )
        None_host = self.addHost( 'h10' )
        SANET_host = self.addHost( 'h11' )
        CESNET_host = self.addHost( 'h12' )
        Klagenfurt2_host = self.addHost( 'h13' )
        Graz1_host = self.addHost( 'h14' )
        Linz1_host = self.addHost( 'h15' )
        Linz2_host = self.addHost( 'h16' )
        Salzburg1_host = self.addHost( 'h17' )
        Salzburg2_host = self.addHost( 'h18' )
        Innsbruck1_host = self.addHost( 'h19' )
        Innsbruck2_host = self.addHost( 'h20' )
        Dornbirn_host = self.addHost( 'h21' )
        Klagenfurt1_host = self.addHost( 'h22' )

        # add edges between switch and corresponding host
        self.addLink( Eisenstadt , Eisenstadt_host )
        self.addLink( St.Polten , St.Polten_host )
        self.addLink( Graz2 , Graz2_host )
        self.addLink( Leoben , Leoben_host )
        self.addLink( Vienna2 , Vienna2_host )
        self.addLink( GEANT , GEANT_host )
        self.addLink( Krems , Krems_host )
        self.addLink( Vienna1 , Vienna1_host )
        self.addLink( Level3 , Level3_host )
        self.addLink( VIX , VIX_host )
        self.addLink( None , None_host )
        self.addLink( SANET , SANET_host )
        self.addLink( CESNET , CESNET_host )
        self.addLink( Klagenfurt2 , Klagenfurt2_host )
        self.addLink( Graz1 , Graz1_host )
        self.addLink( Linz1 , Linz1_host )
        self.addLink( Linz2 , Linz2_host )
        self.addLink( Salzburg1 , Salzburg1_host )
        self.addLink( Salzburg2 , Salzburg2_host )
        self.addLink( Innsbruck1 , Innsbruck1_host )
        self.addLink( Innsbruck2 , Innsbruck2_host )
        self.addLink( Dornbirn , Dornbirn_host )
        self.addLink( Klagenfurt1 , Klagenfurt1_host )

        # add edges between switches
        self.addLink( Eisenstadt , Vienna2, bw=10, delay='0.20981266087676062ms')
        self.addLink( Eisenstadt , Vienna1, bw=10, delay='0.20981266087676062ms')
        self.addLink( St.Polten , Krems, bw=10, delay='0.11843864786318808ms')
        self.addLink( St.Polten , Vienna1, bw=10, delay='0.27825993708723223ms')
        self.addLink( Graz2 , Leoben, bw=10, delay='0.22373664978675145ms')
        self.addLink( Graz2 , Graz1, bw=10, delay='0.0ms')
        self.addLink( Graz2 , Vienna1, bw=10, delay='0.7345348957682828ms')
        self.addLink( Leoben , Graz1, bw=10, delay='0.22373664978675145ms')
        self.addLink( Vienna2 , Krems, bw=10, delay='0.30660785694041964ms')
        self.addLink( Vienna2 , None, bw=10, delay='6.8228013685464e-07ms')
        self.addLink( Vienna2 , Klagenfurt2, bw=10, delay='1.193833578417837ms')
        self.addLink( Vienna2 , Graz1, bw=10, delay='0.7345348957682828ms')
        self.addLink( Vienna2 , Linz1, bw=10, delay='0.7867010554321844ms')
        self.addLink( Vienna2 , Salzburg2, bw=10, delay='1.2792298434073228ms')
        self.addLink( Vienna2 , Innsbruck1, bw=10, delay='1.9653176559128034ms')
        self.addLink( GEANT , None, bw=10, delay='6.8228013685464e-07ms')
        self.addLink( Vienna1 , None, bw=10, delay='6.8228013685464e-07ms')
        self.addLink( Vienna1 , Linz2, bw=10, delay='0.7867010554321844ms')
        self.addLink( Vienna1 , Salzburg1, bw=10, delay='1.2792298434073228ms')
        self.addLink( Vienna1 , Innsbruck2, bw=10, delay='1.9653176559128034ms')
        self.addLink( Vienna1 , Klagenfurt1, bw=10, delay='1.193833578417837ms')
        self.addLink( Level3 , None, bw=10, delay='6.8228013685464e-07ms')
        self.addLink( VIX , None, bw=10, delay='6.8228013685464e-07ms')
        self.addLink( None , SANET, bw=10, delay='6.8228013685464e-07ms')
        self.addLink( None , CESNET, bw=10, delay='6.8228013685464e-07ms')
        self.addLink( Klagenfurt2 , Klagenfurt1, bw=10, delay='0.0ms')
        self.addLink( Linz1 , Linz2, bw=10, delay='0.0ms')
        self.addLink( Salzburg1 , Salzburg2, bw=10, delay='0.0ms')
        self.addLink( Innsbruck1 , Innsbruck2, bw=10, delay='0.0ms')
        self.addLink( Innsbruck1 , Dornbirn, bw=10, delay='0.6421298286062107ms')
        self.addLink( Innsbruck2 , Dornbirn, bw=10, delay='0.6421298286062107ms')

topos = { 'generated': ( lambda: GeneratedTopo() ) }

# HERE THE CODE DEFINITION OF THE TOPOLOGY ENDS

# the following code produces an executable script working with a remote controller
# and providing ssh access to the the mininet hosts from within the ubuntu vm
controller_ip = ''

def setupNetwork(controller_ip):
    "Create network and run simple performance test"
    # check if remote controller's ip was set
    # else set it to localhost
    topo = GeneratedTopo()
    if controller_ip == '':
        #controller_ip = '10.0.2.2';
        controller_ip = '127.0.0.1';
    net = Mininet(topo=topo, controller=lambda a: RemoteController( a, ip=controller_ip, port=6633 ), host=CPULimitedHost, link=TCLink)
    return net

def connectToRootNS( network, switch, ip, prefixLen, routes ):
    "Connect hosts to root namespace via switch. Starts network."
    "network: Mininet() network object"
    "switch: switch to connect to root namespace"
    "ip: IP address for root namespace node"
    "prefixLen: IP address prefix length (e.g. 8, 16, 24)"
    "routes: host networks to route to"
    # Create a node in root namespace and link to switch 0
    root = Node( 'root', inNamespace=False )
    intf = TCLink( root, switch ).intf1
    root.setIP( ip, prefixLen, intf )
    # Start network that now includes link to root namespace
    network.start()
    # Add routes from root ns to hosts
    for route in routes:
        root.cmd( 'route add -net ' + route + ' dev ' + str( intf ) )

def sshd( network, cmd='/usr/sbin/sshd', opts='-D' ):
    "Start a network, connect it to root ns, and run sshd on all hosts."
    switch = network.switches[ 0 ]  # switch to use
    ip = '10.123.123.1'  # our IP address on host network
    routes = [ '10.0.0.0/8' ]  # host networks to route to
    connectToRootNS( network, switch, ip, 8, routes )
    for host in network.hosts:
        host.cmd( cmd + ' ' + opts + '&' )

    # DEBUGGING INFO
    print()
    print("Dumping host connections")
    dumpNodeConnections(network.hosts)
    print()
    print("*** Hosts are running sshd at the following addresses:")
    print()
    for host in network.hosts:
        print(host.name, host.IP())
    print()
    print("*** Type 'exit' or control-D to shut down network")
    print()
    print("*** For testing network connectivity among the hosts, wait a bit for the controller to create all the routes, then do 'pingall' on the mininet console.")
    print()

    CLI( network )
    for host in network.hosts:
        host.cmd( 'kill %' + cmd )
    network.stop()


if __name__ == '__main__':
    setLogLevel('info')
    #setLogLevel('debug')
    sshd( setupNetwork(controller_ip) )
